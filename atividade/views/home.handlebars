<h1>Usuários Cadastrados</h1>
<div class="container">
    {{#each users}}

    <div class="item">
      <h2>{{this.name}}</h2>
      <p>{{this.occupation}}</p>
      <div class="actions">
        <a href="/users/{{this.id}}" class="datails-btn">Ver detalhes</a>
            {{!-- CAMINHO QUE OS DADOS VAI SEGUIR --}}
        <a href="/users/edit/{{this.id}}" class="edit-btn">Editar</a>
           {{!-- CAMINHO QUE OS DADOS VAI SEGUIR --}}

        <form action="/users/delete/{{this.id}}" method="post">
          <input type="submit" value="excluir" class="delete-btn">
        </form>

      </div>
    </div>
    {{/each}}
</div>